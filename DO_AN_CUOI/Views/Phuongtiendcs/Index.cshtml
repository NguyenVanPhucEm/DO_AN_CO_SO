@model IEnumerable<DO_AN_CUOI.Models.Phuongtiendc>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                <p>Tên Phương Tiện</p>
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Tenpt)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Mapt">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Mapt">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Mapt">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
